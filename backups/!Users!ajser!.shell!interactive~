# put your aliases here
alias emacs-here="emacs . &"
alias edit-zsh="emacs ${HOME}/.zsh &"

# if in a terminal
if [ -t 0 ]; then
    # other users shouldn't be able to message by default
    mesg n

    # enable colours
   if [ `tput colors` -ge 8 ]; then
   fi
fi
